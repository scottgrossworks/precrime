# Scoring System

Canonical scoring policy lives in `DOCS/SCORING.json`.

Do not maintain scoring weights, gates, generic-email lists, or `leed_ready`
requirements in this Markdown file. It exists only as a pointer for older notes
that referenced `SCORING_SYSTEM.md`.

