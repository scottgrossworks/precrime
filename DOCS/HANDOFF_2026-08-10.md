# PRECRIME — HANDOFF 2026-08-10 (newest authority, read this first)

This file is the top authority. `DOCS/START_HERE.md` (2026-06-07) and everything
it points to (`STATUS.md`, `STARTUP.md`, the June "procedural conductor" redesign)
describe the architecture BEFORE the reasons-first mining system existed. Treat
those as historical background only, not current fact. `DOCS/Claude.md` (the
hard rules) and `DOCS/FOUNDATION.md` (the demand-signal doctrine) are still
current and load-bearing; read both.

## 0. Paths (edit BOTH trees, every change)
- Source of truth (git repo, commit + push here): `C:\Users\Scott\Desktop\WKG\PRECRIME`
- Deployed / what runs live: `C:\Users\Scott\Desktop\WKG\TDS\precrime`
- Workflow: edit in TDS first, `node --check` + live smoke test, hash-verify
  sync to PRECRIME (`diff -q`), then `git commit` + `git push --all` from PRECRIME.

## 1. Bootstrap read order for a fresh session
1. `DOCS/HANDOFF_2026-08-10.md` — this file
2. `DOCS/Claude.md` — hard rules (simplicity, surgical changes, read-before-act)
3. `DOCS/FOUNDATION.md` — the demand-signal doctrine (why HOT LEEDZ requires
   evidence, not plausibility)
4. `DOCS/solutions/**/*.md` — the CE-style solved-bugs library (frontmatter by
   category: logic-errors, architecture-patterns, integration-issues). Check
   here before re-diagnosing something. NOTE: this library was NOT updated
   during the 2026-08-08/09 session — the fixes below (mining priority,
   mark_sent sync, judge evidence bar, fabrication guard, Tavily freeze) have
   no solutions/ entry yet. If you re-solve one of these from scratch, that is
   why; consider backfilling an entry.
5. `git log --oneline -20` in PRECRIME — the commit messages ARE a changelog
   of everything below, in order.

Skip `DOCS/START_HERE.md`, `STATUS.md`, `STARTUP.md`, `REDESIGN_2026-06-07.md`
unless you need pre-August architecture archaeology.

## 2. Current architecture (as of commit c421d44)

**The one goal:** HOT LEEDZ. A hot leed = a Booking with a real future date +
venue + zip AND quotable evidence of demand or buying precedent (procedural
completeness alone is explicitly banned from scoring hot — see
`DOCS/PROMPTS.json` judge rules).

**Priority order, enforced in the conductor:** mine the ~2,900 existing
clients for creative re-contact reasons FIRST. Tavily/web discovery is last
resort, currently frozen at zero spend (section 4).

Reasons-first mining loop:
- `server/mcp/reasons/ReasonGenerator.js` — LLM emits seasonal/trajectory/
  referral re-contact reasons as Factlet rows (`source: reason:<kind>`),
  prompt in `REASON_PROMPT.json`, 2h cooldown.
- `server/mcp/reasons/ReasonPlanner.js` — Stage 1 of plan_tasks, turns reasons
  into `MINE_REASON` tasks, suppresses SCRAPE_SOURCE/DISCOVER_SOURCES/
  DRILL_CONTAINER/DRILL_DOWN/LAST_30_DAYS while mining is active. Failed hunts
  retry up to 2x via a failCount map (not "hunted forever").
- `skills/mine-reason.md` + `workerManifest.js` MINE_REASON entry — a spawned
  goose curiosity worker hunts backwards from a reason through `find` DB
  queries to affected clients. Deliberately kept as a multi-turn goose session
  (not collapsed to cheap completions) per explicit user decision — the
  reasoning-token loop is the work, and is the workload most portable to
  future local-model hardware. Do not propose eliminating this LLM step.
- `server/mcp/db.js` `conductorGetReadyTasks()` — while any MINE_REASON task
  is ready/claimed, dispatch ONLY mining tasks, ahead of drills.

Outreach: never freehanded by the chat model. `pipeline enqueue
type:"DRAFT_OUTREACH" bookingId:<id>` routes through the in-process
`server/mcp/workers/DraftOutreachWorker.js`, which drafts (dossier-aware,
re-hire framing when applicable), creates the Gmail draft (draft-only
endpoint, never sends), and synchronously `await`s `mark_sent` at the moment
of drafting. `skills/show-hot-leedz.md` forbids the chat model from calling
`gmail__gmail_send`, `get_config`, or composing email text.

Prompt externalization (standing rule): every LLM prompt lives in an adjunct
JSON file next to its consumer (`{lines:[...], _comment}`), loaded via
`server/mcp/promptLoader.js` `fillPrompt(lines, map)` (function-based
replacement, never string replacement — string replacement corrupts on `$`
patterns). New prompts must follow this pattern; do not inline a prompt string
into a `.js` file.

Model resolution (standing rule): `_llmComplete` in `factlets.js` resolves
`(role && cfg.llmModels[role]) || cfg.llmModel || null`. No hardcoded fallback
models anywhere. Missing config = loud `console.error` + `return null`, never
a silent default. Roles: `judge`, `gate`, `apply`, `draft`, `reason` in
`precrime_config.json` `llm.models`.

## 3. Data state
- ~2,900 clients total, including 209 CA schools imported 2026-08-09 via
  `scripts/import_ca_schools.js` (one-best-contact-per-school, denomination
  tagged `segment: school:jewish|catholic|christian|muslim` with a
  `[PERMANENT]` dossier line steering outreach to the right calendar and away
  from secular Halloween pitches). Import script is idempotent-safe to
  re-read but was a ONE-SHOT run; do not re-run against the same legacy file
  without checking for dupes first.
- Own-book clients (`source` starting `square:` or `gmail`) are PERMANENTLY
  excluded from Tavily search — they are private individuals, not web-findable
  organizations. This is a code rule in `mcp_server.js` Stage 5, not just a
  budget setting.

## 4. Tavily/web spend — freeze LIFTED 2026-08-10 (user-directed)
The emergency zeros are GONE: the five task types (FIND_CLIENT_SOURCES,
DRILL_DOWN, DRILL_CONTAINER, DISCOVER_SOURCES, SCRAPE_SOURCE) were deleted
from `tasks.limits`/`tasks.sessionBudgets` in both `TDS/precrime/` and
`TDS/TMP/` configs, so the code's mine-first Move 2 defaults apply
(DRILL_DOWN 3/8, DRILL_CONTAINER 1/2, FIND 5/10, SCRAPE 5/25, DISCOVER 1/1).
User's standing rule: NEVER pin budgets to 0 — the mine-first ratio is
enforced by ReasonPlanner lane suppression (outbound lanes pause while
mining is active), not by zeros. The own-book Tavily exclusion (square:/gmail
clients never web-searched, mcp_server.js Stage 5) remains in force.

## 5. Known open items (not yet done, do not assume done)
- **Reporting-clarity overhaul** — PARTIALLY started 2026-08-10: the status
  report now (a) splits BREWING into real prospects vs auto-minted "(seed)"
  stubs, and (b) has an OUTREACH-READY section — bookings a DRAFT_OUTREACH
  could target right now (hot OR brewing + unshared + future date + client
  email; seeds excluded, Re-engagement rows included), soonest-first with
  bookingIds the user can quote to enqueue a draft. Drafting stays
  USER-TRIGGERED ONLY (2026-08-10 decision: user babysits every draft,
  often supplies a template; never auto-plan DRAFT_OUTREACH). The rest of
  the overhaul (conductor log lines saying what subsystem produced a fact
  and why it matters) is still open.
- **Stage 3 hot-gate deadlock (diagnosed 2026-08-10, intentionally NOT
  auto-fixed)** — the planner's entire act stage (`if (hotExists > 0)`,
  mcp_server.js Stage 3) is unreachable at 0 hot, and in interactive mode
  it only ever plans SHOW_HOT_LEEDZ anyway. Known and accepted for now:
  the user drives outreach by hand off the OUTREACH-READY list. Revisit if
  hands-off operation is ever wanted.
- **Event-as-Client structural gap** — a dateless event/org (e.g. "FIFA World
  Cup 26 Los Angeles") can be saved as a Client, and nothing date-based can
  ever prune it because Client rows have no date field. One specimen so far,
  handled manually (`ignore <name>`). No code fix built; revisit if it
  recurs.
- solutions/ backfill for this session's fixes (see section 1, item 4).

## 6. Verifying you're not stale
Run `git log --oneline -20` in PRECRIME. If you see commits after `c421d44`
that this file doesn't mention, this file itself is now stale — read the
newer commit messages before trusting anything above.
