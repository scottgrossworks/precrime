# BLACKLIST -- permanent ignore list. One company/person per line.
# Anything here is dismissed, never re-discovered, never re-drilled, never presented.
# Add entries by hand or by telling the orchestrator: "ignore <name>".
# Keep terms SPECIFIC (substring-matched): a company name yes, an event name no
# (banning the event would kill every other lead at it).
