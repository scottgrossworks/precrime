# Dependencies
node_modules/
DEV/marks/venv/
DEV/marks/__pycache__/

# Separate git repos — not tracked here
TMP/DEV/
TMP/LOCAL/
TMP/SCRAPERS/

# Environment / secrets
.env
*.env.*
the-leedz-bf438f44eef5.json

# Scraped data (output, not source)
scrapes/

# Databases (live data, not source)
*.sqlite
*.sqlite.backup
*.sqlite-shm
*.sqlite-wal
# Keep the empty schema template
!data/template.sqlite

# Logs
*.log
logs/

# Python
__pycache__/
*.pyc
*.pyo

# Claude Code local settings
.claude/

# Binary installers
*.exe
*.msi
*.dmg
*.pkg

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/

# Scratch — any TMP dir at any depth (root, scripts/TMP, templates/skills/TMP)
TMP/

# Live runtime config (holds API keys — track precrime_config.sample.json instead)
precrime_config.json

# SQLite journal (transient, was leaking server/data/)
*.sqlite-journal

# Business data — peer source lists carry trade-specific keywords, never source
PEER_SOURCES.json

# last30days/ is a separate nested git repo and holds a live API key — never tracked here
last30days/
